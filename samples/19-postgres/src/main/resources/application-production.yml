management:
  endpoints:
    web:
      exposure:
        include: "health,metrics"
  endpoint:
    health:
      show-details: "never"

server:
  port: 9443
  error:
    include-stacktrace: "never"

springdoc:
  swagger-ui:
    enabled: false

spring:
  application:
    name: "thymeleaf-form-validation"
  messages:
    basename: "i18n/messages"
  threads:
    virtual:
      enabled: true

logging:
  config: "classpath:log/logback-spring.xml"

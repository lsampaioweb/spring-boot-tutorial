management:
  endpoints:
    web:
      exposure:
        include: "health,metrics"
  endpoint:
    health:
      show-details: "never"

server:
  error:
    include-stacktrace: "never"

app:
  websocket:
    allowed-origins:
      - "${WEBSOCKET_ALLOWED_ORIGIN:https://client.example.com}"

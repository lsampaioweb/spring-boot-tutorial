server:
  port: 8080

spring:
  rabbitmq:
    host: localhost
    port: 5672
    virtual-host: /
    username: admin
    password: admin
    # Disable auto-recovery for tests to avoid connection hanging.
    connection-timeout: 1000

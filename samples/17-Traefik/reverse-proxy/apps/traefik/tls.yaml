tls:
  options:
    tls-loadbalancer:
      minVersion: "VersionTLS13"
  # certificates:
  #   - certFile: "/etc/traefik/certificates/loadbalancer.homelab.crt"
  #     keyFile: "/etc/traefik/certificates/loadbalancer.homelab.key"
